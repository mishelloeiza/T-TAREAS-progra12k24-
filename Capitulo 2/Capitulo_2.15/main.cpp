#include <iostream>
using namespace std;
int main() {

    // Problema a)
    int x ;

   x = -7 + 3 * 6 / 2 - 1;
    // Salida del problema a
    std::cout << "Resultado a): x = " << x << std::endl;

    // Problema b)
    // Se inicializa otra vez la variable
   // x = 2 % 2 + 2 * 2 - 2 / 2;
      // Salida del problema b
//    std::cout << "Resultado b): x = " << x << std::endl;

    // Problema c)
    // Se inicializa otra vez la variable
//    x = (3 * 9 * (3 + (9 * 3 / (3))));
      // Salida del problema c
 //   std::cout << "Resultado c): x = " << x << std::endl;

    return 0;
}
