#include <iostream>
//Mishel loeiza 9959-23-3457
using namespace std;

int main()
{
    //a) Recibir como entrada la variable unsigned int x con ciny >>.

        cin>>x;
    //b) Recibir como entrada la variable unsigned int y con ciny >>.
        cin>>y;

    //c) Declarar la variable unsigned int i e inicializarla con 1.
        unsigned int i;
        i=1;

    //d) Declarar la variable entera potencia e inicializarla con 1.
       unsigned int potencia;
        potencia=1;
    //e) Multiplicar la variable potencia porx y asignar el resultado a potencia.
          potencia*=x;
    //f) Preincrementar la variable i en 1.
        ++i
    //g) Determinar si 1 es menor o igual a y.
        if (i<=y)
    //h) Imprimir la variable entera potencia con cout y <<.

    cout<<potencia <<endl;










    return 0;
}
