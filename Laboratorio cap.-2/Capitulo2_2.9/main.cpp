#include <iostream>

using namespace std;

int main()
{
// a) Imprimir el mensaje "Escriba dos números".
    //std::cout << "Escriba dos números: ";

// b) Asignar el producto de las variables `b` y `c` a la variable `a`.
    int a,b,c;
    a=0;
    b=0;
    c=0;
    a = b * c;

// c) Indicar que un programa va a realizar un cálculo de nómina.
    // Este programa calcula el salario bruto de un empleado

// d) Recibir tres valores de entrada del teclado y colocarlos en las variables enteras `a`, `b` y `c`.
    std::cin >> a >> b >> c;

}
